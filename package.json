{
  "name": "quad-interactive-fader",
  "version": "1.0.0",
  "description": "Quad Interactive Mix Fader",
  "scripts": {
    "start": "http-server -p 8080 -c-1 --cors",
    "dev": "http-server -p 8080 -c-1 --cors -o"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
